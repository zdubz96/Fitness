// Bumped manually on notable releases so the running build is identifiable in Settings.
// If the app shows an older value than expected, the device is serving a cached version.
export const APP_VERSION = "12 — Health tab redesign (axes, stat cards, insights)";
